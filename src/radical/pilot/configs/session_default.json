
# This is the default client session config which is used if no other config is
# specified.  It contains the minimal number of settings required for a
# functional rp session.

{
    "bridges" : {
        "log_pubsub"     : {"log_lvl"   : "debug",
                            "stall_hwm" : 1,
                            "bulk_size" : 0},
        "state_pubsub"   : {"log_lvl"   : "debug",
                            "stall_hwm" : 1,
                            "bulk_size" : 0},
        "control_pubsub" : {"log_lvl"   : "debug",
                            "stall_hwm" : 1,
                            "bulk_size" : 0}
    }
}


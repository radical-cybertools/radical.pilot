# this is the default session config which is used if no other config is
# specified.  It contains the minimal set of settings required for
# a functional rp session.
{
    # NOTE: some of the bridges could also go to the UMGR or PMGR.
    "bridges" : {
        "umgr_staging_input_queue"  : { "log_level" : "off",
                                        "stall_hwm" : 1,
                                        "bulk_size" : 1},
        "umgr_scheduling_queue"     : { "log_level" : "off",
                                        "stall_hwm" : 1,
                                        "bulk_size" : 1},
        "umgr_staging_output_queue" : { "log_level" : "off",
                                        "stall_hwm" : 1,
                                        "bulk_size" : 1},

        "umgr_unschedule_pubsub"    : {"log_level" : "off"},
        "umgr_reschedule_pubsub"    : {"log_level" : "off"},

        "pmgr_launching_queue"      : { "log_level" : "off",
                                        "stall_hwm" : 1,
                                        "bulk_size" : 1},

        "control_pubsub"             : {"log_level" : "off"},
        "state_pubsub"               : {"log_level" : "off"}
    },
    "components" : {
        # how many instances of the respective components should be started
        "UpdateWorker" : 1
    }
}


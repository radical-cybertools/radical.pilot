
language: python
python:   "2.7"

before_install:
  - uname -a
  - ulimit -a
  - python -V
  - pip install pytest coverage codecov flake8

install:
  - pip install .

script:
  - python setup.py test

notifications:
  email:
    recipients: andre@merzky.net
    on_success: change
    on_failure: always

after_success:
  - after_success:
      - coverage combine; \
        coverage xml; \
        coverage report;

